# CH9140Bridge 代码审核修改意见

> 审核范围: `CH9140Bridge/` 全部 18 个源文件(约 3400 行)
> 实证验证: 构建通过 / 65 项自检全部通过 / TSan 自检路径无报警
> 审核日期: 2026-09-13

整体评价: 代码质量较高——协议编解码有测试向量对照、BLE 状态机覆盖了超时/串流/转连边界、
PTY 写入有背压与溢出保护、UI 层有合帧节流。以下为发现的问题, 按优先级分组。

---

## P0 功能性 Bug(建议优先修复)

### 1. 自动重连在连接失败时"承诺了但永不执行"

- **位置**: `Sources/CH9140Core/BridgeModel.swift:192-200`
- **现象**: `.disconnected` 与 `.failed` 走同一分支, 先打印"2 秒后尝试自动重连",
  但延迟闭包的守卫是 `connectionState == .disconnected`。`.failed` 是终态
  (没有任何代码把它改回 `.disconnected`), 因此连接超时/失败后重连**从不发生**,
  日志却承诺会重连。
- **修改建议**: 二选一——
  - 守卫放行失败态: `guard self.ble.connectionState == .disconnected || …` 改为
    排除进行态即可, 如 `guard ![.connecting, .discovering, .ready].contains(state)`;
  - 或把 `.failed` 移出重连分支, 不再打印重连承诺。

### 2. 断连期间的发送数据会在下次连接时"复活"

- **位置**: `Sources/CH9140Core/BLE/BLEManager.swift:234-247`(`send`)、`:180-208`(`startConnecting`)
- **现象**: `send()` 不检查连接状态, 断连期间(如串口工具仍往虚拟串口写)数据积压在
  `outbox`(上限 256KB)。`didDisconnectPeripheral` 只在断连瞬间清空一次, 之后的积压
  不会被清除——下次连接 `ready` 后 `pumpOutbox()` 会把**陈旧数据发给新连接的设备**。
- **修改建议**: 在 `startConnecting()` 或 `didConnect` 回调里无条件 `outbox.removeAll()`
  并复位 `chipFullFlag`; 或 `send()` 入口判断非 ready 直接丢弃并计数。

### 3. 版本号漂移

- **位置**: `Resources/Info.plist` `CFBundleShortVersionString = 1.0.0`
- **现象**: git 历史已到 v1.0.4, README 引用 v1.0.1+ 的修复, 但 App 版本号从未递增。
- **修改建议**: 版本号提升到与发布一致; 建议在 `build_app.sh` 中从 git tag 自动注入,
  避免再次漂移。

---

## P1 数据竞争(窗口小但真实存在)

### 4. `connectionState`: 主线程写 / bleQueue 读

- **位置**: 写 `BLEManager.swift:355-360`(`setState` 只在主线程);
  读 `BLEManager.swift:267,294`(`applySerialParameters`/`applyModemLines` 在 bleQueue 上
  读 `self.connectionState == .ready`)
- **修改建议**: 状态读写统一到同一线程。推荐: `connectionState` 保持主线程写,
  bleQueue 上的 guard 改为读取一个仅在 bleQueue 维护的私有 `readyFlag`,
  在 `didDiscoverCharacteristicsFor` 就绪处与 `didDisconnectPeripheral` 处同步维护。

### 5. `connectedUUID`: bleQueue 写 / 主线程读

- **位置**: 写 `BLEManager.swift:153,171,202,219,467,485`(bleQueue);
  读 `DeviceListView.swift:144,148,226`、`BridgeModel.swift:176`(主线程)
- **现象**: UUID 为 128 位结构体, 跨线程无同步读写理论上可能读到撕裂值
  (表现为设备行高亮/按钮状态错乱)。
- **修改建议**: 改为 `@Published public private(set)` 并把全部写挪到主线程
  (与 `connectionState` 同一策略), 或加锁保护。

### 6. `VirtualSerialPort.running` 无同步

- **位置**: 写 `VirtualSerialPort.swift:158`(`close()`, 任意线程);
  读 `:251`(`pollLoop` 线程)
- **修改建议**: 改用 `Atomic`/`NSLock` 保护, 或直接用 `Thread.isCancelled`
  作为唯一退出信号(`close()` 已调用 `worker?.cancel()`)。

### 7. `VirtualSerialPort.lastTermios` 跨线程

- **位置**: 写 `VirtualSerialPort.swift:114`(`open()`, 调用方线程);
  读写 `:296-297`(`checkTermios`, poll 线程)
- **修改建议**: 纳入 `stateLock` 保护, 或仅在 poll 线程内初始化
  (把首次 `tcgetattr` 挪进 `pollLoop` 开头)。

### 8. `VirtualSerialPort.masterFD/slaveFD` 赋值不对称

- **位置**: `open()` 中 99-100 行**无锁**赋值; `close()` 中 168-170 行**加锁**赋值
- **修改建议**: 两侧统一经 `stateLock` 读写。这正是 inboundBuffer 注释里
  声称已修好的同类问题, 建议一并梳理。

---

## P2 健壮性

### 9. 终端增量删除缺乏防御, 失配即崩溃

- **位置**: `Sources/CH9140Bridge/Views/TerminalView.swift:93,115`
- **现象**: `storage.deleteCharacters(in:)` 依赖 `render()` 与已渲染内容严格一致;
  一旦失配(未来改 render 逻辑/字体度量变化), `NSRange` 越界直接 crash。
- **修改建议**: 删除前对 range 做 clamp(`min(length, storage.length - location)`),
  失配时兜底 `fullRedraw`。

### 10. PTY 波特率映射上限 230400, 与 UI 的 1M 矛盾

- **位置**: `Sources/CH9140Core/Serial/VirtualSerialPort.swift:326-353`
- **现象**: 串口工具设 460800+ 时 `baudRate(from:)` 返回 nil → 回落 9600 →
  **把错误波特率同步给芯片**; 而面板允许选到 1M。
- **修改建议**: 映射失败时不要静默回落, 记录日志"该波特率超出 PTY 可表达范围,
  未同步"; 或在文档中说明 macOS PTY 标准 speed 上限。

### 11. UserDefaults 读取用 clamping 且无合法性校验

- **位置**: `Sources/CH9140Core/Settings/SettingsStore.swift:150-157`
- **现象**: 损坏值(如 dataBits=300)被 clamp 成 255 后直接下发芯片。
- **修改建议**: 读取后做范围校验(波特率 300~1_000_000、数据位 5-8、停止位 1-2、
  校验 0-4), 越界回退默认值; 可给 `SerialParameters` 加 `validated()`。

### 12. 自检程序污染真实用户设置

- **位置**: `Sources/CH9140SelfTest/main.swift:423-448`
- **现象**: 直接清除/改写 `UserDefaults.standard` 中 `CH9140Bridge.*` 键,
  在装有正式 App 的机器上跑自检会干扰用户配置。
- **修改建议**: `SettingsStore` 支持注入 `UserDefaults` 实例, 自检改用
  `UserDefaults(suiteName: "CH9140SelfTest")`, 结束后 `removePersistentDomain`。

---

## P3 次要问题(可选修)

| # | 问题 | 位置 | 建议 |
|---|------|------|------|
| 13 | `disconnect()` 主动 `setState(.disconnected)` 后, `didDisconnectPeripheral` 又设一次 → `onConnectionChange` 触发两次、`closeSession` 调两次 | `BLEManager.swift:227,493` | 去掉主动 setState, 统一由回调落定; 挂起中连接无回调的分支单独处理 |
| 14 | `LineAssembler.flushPending()` 生产代码从未调用(仅自检用) | `LineAssembler.swift:68` | 删除或改为 internal; 或在 `BridgeModel.flushPendings()` 中真正吐出残行 |
| 15 | CLI 模式 ready 之后设备断开不退出也不重连, 进程变僵尸 | `CLIRunner.swift:71-96` | `.disconnected` 时退出非零码, 或复用 GUI 的重连逻辑 |
| 16 | 交互模式(ConsoleKeyView)不支持 Cmd+V 粘贴 | `ConsoleKeyView.swift:80` | `keyDown` 开头判断 `.command` + "v" 时读 `NSPasteboard` 发送 |
| 17 | `InputSourceGuard.leave()` 覆盖用户在聚焦期间手动切换的输入法 | `InputSourceGuard.swift:42` | leave 前比较当前输入源, 若已被用户改动则不再恢复 |
| 18 | README 写"自检 30 项断言"实际 65 项; 故障排查表缺 v1.0.3/v1.0.4 条目(docs/ 有文档) | `README.md:35,106` | 同步数字与表格 |

---

## 修复优先级建议

1. **第一批(功能正确性)**: #1 自动重连失效、#2 陈旧数据复活、#3 版本号
2. **第二批(线程安全)**: #4/#5 BLE 状态跨线程、#6/#7/#8 PTY 锁一致性
3. **第三批(健壮性)**: #9 终端删除防御、#10 波特率回落、#11 设置校验、#12 自检隔离
4. **第四批(体验与文档)**: #13~#18
