# CH9140Bridge 真机测试报告（2026-09-14）

> 测试人: Codex 自动化测试
> 目的: 设备回线后, 供后续 agent 按本文档复现/验证问题
> 被测版本: CH9140Bridge v1.0.5 (build 12), 构建于 2026-09-14 12:00(与当前源码一致, 已核对编译产物晚于全部源码 mtime)

---

## 0. 测试环境

| 项 | 值 |
|---|---|
| BLE 模块 | CH9140BLE2U (系统 MAC `DC:04:5A:5E:12:5B`, CoreBluetooth UUID `53ECEF71-1DCF-5247-7FAC-5C8E8101361B`) |
| UART 对端 | Linux 交换机, 固件 `SW2.7.1 BUILD20200721-191606`, Console 波特率 **115200 8N1**, 提示符 `Switch#`, 帮助指令 `list` |
| 写入 MTU | 77 字节(连接日志可见) |
| 测试侧设置 | `applyDefaultsOnConnect=0`(连接后不下发), `followVirtualPortBaud=1`(波特率跟随开), 日志按日期分目录, 模板 `CH9140_{device}_{datetime}{seq}` |
| 测试手段 | `swift run CH9140SelfTest`(69 项)、pyserial 3.5 经虚拟串口、AppleScript GUI 自动化、screencapture 截图核对 |
| 中途事件 | ~13:07 用户摘掉 UART 对端交换机(此后 RX 无响应, 属预期), BLE 链路始终保持 |

**自检结果: 69/69 全部通过。**

---

## 1. 已验证正常的功能(无需重复测试)

| # | 功能 | 验证方式与结果 |
|---|---|---|
| 1 | BLE 扫描/连接/服务发现 | 点击"连接"→ 正在连接 → 已连接 → 透传通道就绪(MTU 77) |
| 2 | 基本收发 | 虚拟串口发 `list\r` → 收到完整 628 字节命令清单, 结尾 `Switch# ` |
| 3 | 波特率跟随(tcsetattr→0xFFF3) | pyserial 以 4800/38400 打开 → 芯片切换、对端变乱码; 回 115200 → 恢复。双向均生效 |
| 4 | 面板"写入芯片"(0x06+0x86 回包校验) | 面板改 9600 写入 → 日志"下发串口配置…串口参数配置成功", 对端失配静默; 写回 115200 恢复 |
| 5 | 压力完整性 | 50ms 间隔连发 50 次 `list` → 50/50 完整响应(31610 字节, 零乱码) |
| 6 | TX 字节级一致 | 同一 200B payload 快发/慢发, 设备响应 SHA256 完全相同(零丢字节) |
| 7 | 大输出 | `list running_config` 1151B/66 行、`list vlan` 表完整收取, 干净收尾 |
| 8 | PTY 反复开关 | 打开→通信→关闭 ×10, 10/10 正常 |
| 9 | 日志切割(截断并新建) | 新文件 `{seq}` 递增(01→02)、含"(手动切割, 序号 2)"标记, 旧文件有"会话结束"收尾 |
| 10 | 日志命名/按日期分目录 | `~/Documents/CH9140Logs/2026-09-14/CH9140_CH9140BLE2U_20260914_HHMMSS01.log` 符合模板 |
| 11 | MODEM 状态灯 | CTS 绿灯(0x88 上报); 状态栏 RSSI(-68~-73 dBm 波动)实时刷新 |
| 12 | 串口占用检测 | pyserial 打开时面板显示"串口工具已占用", 关闭回"空闲/等待串口工具打开" |
| 13 | 关闭/重建虚拟串口 | "关闭串口"→ 符号链接与 /dev/ttysNNN 均清理; "创建虚拟串口"→ 重建成功 |
| 14 | 监视器 | 时间戳、→TX 蓝标、HEX 模式(hex dump 正常)、行计数 |
| 15 | 终端页(SwiftTerm) | 历史渲染正常(含 ANSI/表格), 光标正常 |
| 16 | 发送栏 | AX 设值+发送 → 监视器出现 `→ list`, 最近发送历史图标出现 |
| 17 | CLI 模式 | 扫描→连接→下发 115200→创建 PTY→`CLI_READY port=… compat=…`→每 10s 统计行; 不存在设备 8s 超时退出码 **2**; TX 统计准确(105B) |
| 18 | 最近连接两条同名记录 | **非 bug**: 两条 UUID 不同(53ECEF71…/8BE7B8EA…), 是两块物理模块, 按 UUID 去重逻辑正确 |
| 19 | TX 行显示剥离行尾 | "→ list" 不显示 0D 是刻意设计(TerminalView.swift:181 注释), 非 bug |

---

## 2. 确认的问题(按优先级)

### 问题① 纯文本日志: TX 记录无前缀"隐形", 并污染 RX 行 ★建议优先修

**严重度**: 中(数据未丢, 但日志无法按行解析方向, TX 看似丢失)

**现象**(证据文件 `~/Documents/CH9140Logs/2026-09-14/CH9140_CH9140BLE2U_20260914_12562802.log`):
```
[2026-09-14 12:58:38.999] [RX] Switch# list mode\x0dlist mode\x0d\x0a
```
看似"回显重复"。同时整个会话 `grep "\[TX\]"` 只有 1 条——PTY 发送的 `list mode\r`、`list vlan\r` 都找不到 [TX] 记录。

**同期 PTY 原始字节(ground truth)**: `list mode\r\nFIT MODE\r\nSwitch# ` —— 无重复、无多余 \r。

**根因**(已定位): `Sources/CH9140Core/Logging/SessionLogger.swift` 的 `log()` ASCII+时间戳路径:
- 只有"行首"(上一块数据以 0x0A 结尾)才插 `[时间] [方向] ` 前缀;
- `rxAtLineStart`/`txAtLineStart` 两个标志各自独立, 但写的是**同一个文件流**;
- 当某方向数据不带 \n(提示符 `Switch# `、单字节 `\r`、命令回显)时物理行处于"开放"状态, **另一方向的写入无前缀直接拼进同一行**。

上面的日志行实际构成: `Switch# `(RX 残行) + `list mode\r`(**TX, 无前缀**) + `list mode\r\n`(RX 回显)。TX 字节其实在文件里, 但无标记、无独立行。

**复现**:
1. 连接设备, 开启默认日志(纯文本+时间戳)
2. 用任意串口工具经虚拟串口发 `list\r`(或对端先有未换行的提示符)
3. 检查日志: `grep -c "\[TX\]" <日志>` 远少于实际发送次数; RX 行内可见 TX 内容混入

**修复建议**(任选其一):
- 方向切换时若当前物理行开放, 先补 `\n` 再写带前缀的新行(牺牲一点字节精确, 换可读性);
- 或 RX/TX 各自按行缓冲, 成行后才写文件;
- 或纯文本模式也接受为每条裸数据补前缀。

---

### 问题② 进程退出不清理虚拟串口符号链接(GUI/CLI 均复现)

**现象**:
- GUI ⌘Q 退出后: `~/.ch9140/cu.CH9140` 和 `~/Library/Application Support/CH9140Bridge/cu.CH9140` 仍指向已消失的 `/dev/ttys001`(读取报 No such file or directory);
- CLI 被 SIGTERM(退出码 143)后同样残留;
- 下次启动会 `removeItem` 后重建(能自愈), 但退出期间串口工具打开该路径会失败且难以排查。

**根因**: `VirtualSerialPort.close()` 只在面板主动"关闭串口"时调用; App 终止无 `applicationWillTerminate` 钩子; CLI 无 signal handler。

**验证**: App 运行中 `readlink ~/.ch9140/cu.CH9140` → 记下 ttysNNN → ⌘Q → 链接仍在, 目标已消失。

**修复建议**: GUI 在 `applicationWillTerminate`/`scenePhase` 里调 `port.close()`; CLI 用 `signal()` 或 `DispatchSourceSignal` 捕获 SIGINT/SIGTERM → `port.close()` 后退出。

---

### 问题③ CLI 无 SIGINT/SIGTERM 优雅退出

**现象**: CLI 运行中无信号处理代码(`CLIRunner.swift` 无任何 signal/atexit)。SIGTERM 直接杀死进程, 退出码 143, 链接残留(问题②)。README 未说明如何退出 CLI。

**修复建议**: 捕获 SIGINT/SIGTERM → 清理符号链接 → 以明确退出码(如 0 或 130)退出; README 补充退出方式说明。

附: CLI 另有一处观感问题——`CLI_READY` 打印出现在 `[PTY] 虚拟串口已创建` 日志**之前**(emitLog 异步跳主线程 vs say() 直接打印)。端口实际已就绪, 仅日志顺序误导, 可顺手修(在 open() 内同步回调或 CLI_READY 延后一拍)。

---

### 问题④ GUI 没有"断开连接"入口(易用性缺陷)

**现象**: 经"最近连接"直连设备后:
- 最近连接行只显示绿色对勾(`RecentDeviceRow` 的 `isCurrent` 分支无按钮);
- 已连接的 CH9140 停止广播 → 扫描列表为空(实测扫描显示 0 台) → `DeviceRow` 的"断开"按钮永不出现;
- 菜单栏(串口/日志)无断开项;
- **结果: 用户想断开只能退出整个 App。**

**代码**: `Sources/CH9140Bridge/Views/DeviceListView.swift` RecentDeviceRow(约 219-262 行)、DeviceRow(约 175 行的断开按钮仅在扫描列表生效)。

**修复建议**: 已连接时最近连接行显示"断开"按钮; 或状态栏/菜单栏加"断开连接"。

---

### 问题⑤ 波特率跟随边界: 以 9600 打开串口工具不会触发同步

**现象**: 芯片当前为 115200(且"连接后自动下发"关闭)时, 用串口工具以 **9600** 打开虚拟串口, 芯片**保持 115200 不切**——因为 `checkTermios` 只在 termios **变化**时同步, 而 PTY 初始值就是 9600(`VirtualSerialPort.open()` 显式设为 9600 8N1 并以其为基线), 9600→9600 无变化。
实测: 芯片 115200 时 pyserial 以 9600 打开发 `list`, 依然收到正确响应(证明芯片未切换)。

**影响**: 与 README "串口工具设什么波特率, 芯片就自动切到什么波特率"的表述不符。行为本身(不打扰)可辩护, 但用户若特意想用 9600 会困惑。

**修复建议**(二选一):
- 文档澄清"跟随仅在变更时触发; 与 PTY 默认值 9600 相同的首设不触发";
- 或连接就绪时把当前芯片参数与 PTY 参数对齐一次(注意与 applyDefaultsOnConnect 的语义冲突)。

---

### 问题⑥ 新会话"芯片: 115200/8/1/无"标签凭空出现(未定位, 需验证)

**现象**: 全新启动 App(旧进程已确认退出) → 点连接 → 就绪后, "写入芯片"按钮旁立即显示 **"芯片: 115200/8/1/无"**。该会话**没有任何**"下发串口配置"日志(监视器 5 行: 蓝牙已打开/虚拟串口已创建/正在连接/已连接/透传通道就绪)。

**代码分析结论(按源码不应出现)**:
- 该标签由 `ControlPanelView.swift:99-103` 渲染, 条件是 `model.activeSerial != nil`;
- `activeSerial` 全工程唯一赋值点是 `BridgeModel.swift:281`(applySerialParameters 成功回调);
- 本会话 applySerialParameters 四条可能路径均未发生: 菜单未点、写入芯片未点、`applyDefaultsOnConnect=0`、`onBaudChange` 未触发(PTY 无任何客户端打开过, 且面板编辑值=9600 与标签 115200 不一致);
- 115200 恰好是上一个 CLI 会话写入芯片的值——怀疑存在某种跨进程状态泄漏或芯片主动上报被误接。

**给后续 agent 的 bisect 步骤**:
1. 退出 App → 重开 → **不连接**直接看"写入芯片"旁是否有标签 → 区分 init 期/连接期;
2. 若连接后才出现: 用 HEX 监视或 BLE 调试确认芯片是否在连接后主动上报了 0x86 报文(`BLEManager.handleConfigPacket` 对无 pending 的 0x86 当前是静默丢弃的, 可在此加日志);
3. 若重开即出现: 检查是否有状态恢复/UserDefaults 键被误读。

---

### 问题⑦ 轻微: 日志字节计数器跨切割累计

切割后新文件 1.6KB, 面板仍显示 139.9 KB(整个会话累计)。`SessionLogger.bytesWritten` 切割不清零。易误导。建议切割时归零或标注"会话累计"。

---

## 3. 观察项(无法定性, 记录备查)

1. **CTS 灯在对端拔线后仍为绿**: 可能是模块输入上拉, 无法远端区分; 需硬件配合验证 0x88 上报真实性。
2. **首个 GUI 进程(12:25 启动)于 12:30 前无崩溃日志退出**: DiagnosticReports 无记录, 疑似用户手动关闭/关窗即退; 未复现, 留意即可。
3. **⚠️ 对端是 Linux 串口控制台的 SysRq 风险**(使用注意, 非本软件 bug): 波特率不匹配期间发送的乱码字节触发了对端内核 `SysRq: Show backtrace of all active CPUs` 全栈打印, 且设备 CLI 重印过一次 banner。乱码字节可能被串口控制台解释为 BREAK+命令。现场调试切换波特率时请注意。

## 4. 未覆盖测试项(设备回线后补测)

- [ ] 自动重连: 模块掉电/超出距离后的 2 秒重连(v1.0.5 修复的核心路径, 本次无条件模拟 BLE 掉线)
- [ ] 连接按钮卡灰回归(v1.0.3): 连接一个开着的但无响应的设备(可用另一台主机占住 CH9140 的单连接)
- [ ] 交互模式(ConsoleKeyView): 逐键回显/Tab 补全/↑↓ 历史/Ctrl+C/Cmd+V/退格重绘——需对端在线
- [ ] 终端页键盘直连(SwiftTerm): 本次键入未送达(自动化限制), 需人工或设备在线时验证
- [ ] 设置面板(⌘,)全部选项: 日志格式(纯文本/HEX/HEX+文本)、存储方式三种、模板实时预览、TX 记录开关、时间戳开关
- [ ] 发送栏 HEX 模式、CR/LF/无 行尾切换、仅数据/滚动/清屏
- [ ] 跨午夜日志自动切换(dailyFolder/dailyFile)
- [ ] 多设备转连(连接中点另一台)
- [ ] CLI 退出码 3(建 PTY 失败)/4(就绪后断开清理)——4 需要真实 BLE 断开
- [ ] MODEM DTR/RTS 应用按钮与 0x07 下发(需确认对端线序支持)

## 5. 复现工具箱(给后续 agent)

### 5.1 重要: @电脑(CUA)插件当前不可用
`SkyComputerUseService` 每次被调用即崩溃(EXC_BREAKPOINT, `Array.remove(at:)` 越界), 崩溃报告在 `~/Library/Logs/DiagnosticReports/SkyComputerUseService-*.ips`。**GUI 自动化请改用 AppleScript**(本机已验证可用)。

### 5.2 GUI 自动化(AppleScript + 截图)
```zsh
# 窗口 ID(供 screencapture -l 使用)
cat > /tmp/winid.swift << 'EOF'
import Cocoa
let wl = CGWindowListCopyWindowInfo(.optionOnScreenOnly, kCGNullWindowID) as! [[String: Any]]
for w in wl where (w["kCGWindowOwnerName"] as? String ?? "").contains("CH9140") {
    print("\(w["kCGWindowNumber"]!)|\(w["kCGWindowName"] ?? "")")
}
EOF
swift /tmp/winid.swift
screencapture -x -l<窗口ID> /tmp/shot.png

# 点击(坐标为屏幕坐标, 先 dump 按钮位置)
osascript -e 'tell application "System Events" to tell process "CH9140Bridge" to set frontmost to true'
osascript -e 'tell application "System Events" to tell process "CH9140Bridge" to click at {x, y}'
```
注意: SwiftUI 自定义输入框(InsertableTextField)对 `keystroke` 不敏感, 需 `set focused of tf to true` + `set value of tf to "…"`; 弹窗菜单用方向键+回车选择; 截图是 2x 像素, 坐标与 AX 报告的 pt 是 1:1(AX 即屏幕坐标)。

### 5.3 串口链路测试(pyserial)
```python
import serial, subprocess, time
PORT = subprocess.run(["readlink","/Users/zhu/.ch9140/cu.CH9140"],
                      capture_output=True, text=True).stdout.strip()  # ttysNNN 动态变化, 勿硬编码
ser = serial.Serial(PORT, 115200, timeout=2)
time.sleep(1.0)            # 留给 tcsetattr 巡检(0.5s 周期)+芯片切换
ser.reset_input_buffer()
ser.write(b"list\r")       # 帮助指令; 响应 628B, 以 "Switch# " 结尾
time.sleep(2)
print(repr(ser.read(ser.in_waiting or 1)))
```
注意: **在 Codex exec 里后台启动的进程会随会话结束被杀**——CLI/常驻探针要么放同一 exec 会话内完成全部操作, 要么前台跑+轮询。

### 5.4 对端设备习性(已摸清)
- `list` → 628B 命令清单; `list mode` → `FIT MODE`; `list running_config` → 1151B/66 行; `list vlan` → VLAN 表
- 输入行超过 ~69 字符时, 控制台做横向滚动重绘(`\r<`+窗口内容+空格+`\x08`×N)——**是设备行为, 勿误判为桥接丢字节**(验证方法: 快慢发同 payload 比 SHA256)
- 波特率不匹配: RX 乱码或无数据; 且乱码可能触发对端 SysRq(见 3.3)
- 设备偶发主动日志(openvswitch reconnect 等)
- 连接参数: 115200 8N1 无流控; 芯片断电重启后恢复出厂 115200

### 5.5 关键路径速查
- 日志: `~/Documents/CH9140Logs/yyyy-MM-dd/*.log`(用 `xxd`/`cat -v` 看控制字符)
- 设置: `defaults read cn.wch.CH9140Bridge`
- 符号链接: `~/.ch9140/cu.CH9140`(无空格, 推荐)、`~/Library/Application Support/CH9140Bridge/cu.CH9140`
- 自检: `cd CH9140Bridge && swift run CH9140SelfTest`
- CLI: `CH9140Bridge.app/Contents/MacOS/CH9140Bridge --cli --name CH9140BLE2U --baud 115200`

## 6. 测试期间产生的辅助文件

- 日志证据: `~/Documents/CH9140Logs/2026-09-14/`(其中 `..._12382901.log` 2008-2022 行是问题①的典型证据; `..._12562802.log` 含切割标记)
- 临时脚本: `/tmp/ch9140_test/`(重启即失, 不重要)

**测试结束时的现场状态**: GUI 运行中并已连接 CH9140BLE2U(BLE 在线), 虚拟串口已创建, 日志记录中; UART 对端已摘除。芯片当前参数 115200 8N1 无流控。
